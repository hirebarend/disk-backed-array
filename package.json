{
  "name": "disk-backed-array",
  "version": "0.1.2",
  "description": "Disked backed array is a data structure that combines the benefits of an array with the persistence of disk storage.",
  "main": "dist/index.js",
  "typings": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "lint": "prettier --write --config prettier.config.js --list-different src/**/*.ts",
    "start": "npm run build && node dist/main.js",
    "test": "npm run build && node dist/test.js"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/hirebarend/disk-backed-array"
  },
  "author": "Barend Erasmus",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.2.6",
    "prettier": "^2.8.8",
    "tslint": "^6.1.3",
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "crc-32": "^1.2.2"
  }
}
